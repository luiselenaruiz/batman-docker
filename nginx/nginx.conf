events {}

http {
    upstream backend {
        server web1:80;
        server web2:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;

            # CABECERA PARA IDENTIFICAR BACKEND
            add_header X-Backend $upstream_addr always;
        }
    }
}